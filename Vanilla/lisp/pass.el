(p! pass
  :straight t
  ;; :config
  ;; (defun org-protocol-pass (fname)
    ""
  ;; (let* ((splitparts (org-protocol-parse-parameters fname nil '(:url :type :title :body)))
  ;;        (uri (org-protocol-sanitize-uri (plist-get splitparts :url)))
  ;;        (type (plist-get splitparts :type))
  ;;        (title (plist-get splitparts :title))
  ;;        (body (plist-get splitparts :body))
  ;;        )
    ;; (when (equal type "query")
      
    ;; )
  )
